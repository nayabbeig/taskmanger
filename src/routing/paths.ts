export enum PATHS {
  HOME = "/",
  DASHBOARD = "/dashboard",
  ADD_TASK = "/add_task",
  PENDING_TASKS = "/pending_tasks",
  COMPLETED_TASKS = "/completed_tasks",
  LOGIN = "/login",
  LOGOUT = "/logout",
  SIGNUP = "/signup",
}

export default PATHS;
